(defcfg
  process-unmapped-keys yes
  windows-altgr cancel-lctl-press
)

(defsrc
  caps
  lctl rctl
)

;; Use f24 as hyper key
(deflayer default
  @escctl
  @hyper  @hyper
)

(defalias
  escctl (tap-hold-press 150 350 esc lctl)
  hyper f24
)
